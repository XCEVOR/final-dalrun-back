<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "com.dalrun.dao.GpxDataDao">

	<!-- gpx 데이터 전체 조회-->
    <select id="gpxDataList" resultType="com.dalrun.dto.GpxDataDto">
        SELECT *
        FROM gpxdata
    </select>
    
    <!-- 업로드 시 하나의 기록 insert -->
    <insert id="insertGpxData" parameterType="com.dalrun.dto.GpxDataDto">
    	INSERT INTO gpxdata
    	(fileSeq, diarySeq, memId, latitude, longitude, altitude, mTime)
    	VALUES (#{fileSeq}, #{diarySeq}, #{memId}, #{latitude}, #{longitude}, #{altitude}, #{mTime})
    </insert>
    
</mapper>