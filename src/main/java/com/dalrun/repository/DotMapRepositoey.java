package com.dalrun.repository;

import com.dalrun.domain.DotMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DotMapRepositoey extends JpaRepository<DotMap,Long> {
}

